[tool.poetry]
name = "gdal-fastapi-graph"
version = "0.1.0"
description = ""
authors = ["fakhriy-ramadhan"]

[tool.poetry.dependencies]
python = ">=3.10,<3.13"
fastapi = "^0.110.0"
uvicorn = {extras = ["standard"], version = "^0.29.0"}
psycopg2-binary = "*"
python-dotenv = "^1.0.1"
# gdal = "3.8.4"  # Installed via pip in Dockerfile due to PEP 517 incompatibility

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api" 